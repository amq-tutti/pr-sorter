export type SorterIndexEntry = {
  slug: string;
  title: string;
  description: string;
  tags?: string[];
  deadline?: string;
  localStoragePrefix?: string;
  url?: string;
  iconUrl?: string;
  sourceTitle?: string;
};
